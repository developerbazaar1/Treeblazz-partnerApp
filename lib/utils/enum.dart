enum ProductCategories {
  cannabis,
  vegitable,
  personalCare,
  homeCare,
  drinks,
}

enum PaymentMode { cod, creditCard, debitCard, strip }

enum OrderStatus {
  received,
  accepted,
  processing,
  readyForPickup,
  outForDelivery,
  delivered,
  cancelled,
}
